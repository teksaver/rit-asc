# Rétrospective : Epic 3 — Le Moteur d'Amnésie Bienveillante

**Date :** 2026-07-06
**Epic :** 3
**Statut :** Done

## Participants

Amelia (Developer, facilitatrice), John (Product Manager), Winston (Architect), Murat (Test Architect), Sally (UX Designer), Sylvain (Project Lead).

## Métriques de livraison

- **Stories complétées :** 3/3 (100 %)
  - 3.1 : Manipulation et Affectation des Tâches (Drag & Drop)
  - 3.2 : Le Saut de Cycle (Amnésie Bienveillante)
  - 3.3 : Suggestions d'enrichissement sur report
- **Tests :** 94/94 verts · `oxlint` propre · `vite build` réussi
- **Intégration :** PR #2 (`epic-3` → `main`) mergée le 2026-07-06

## Ce qui a bien marché

- **Cœur produit livré** : le saut de cycle silencieux fonctionne (tâches `must` laissées intactes comme échec assumé, repli propre vers l'Inbox via `UNASSIGNED_PLANNED_DAY_ID`). La valeur "pas de culpabilité, pas de pastille rouge" est codée, pas seulement promise.
- **Test-First tenu sur 3.2** : `cycleJump.test.js` écrit avant l'implémentation, 7 scénarios collés aux critères d'acceptation. → application directe de l'action item n°1 de la rétro Epic 2.
- **La vérification en navigateur réel a payé (3.1)** : elle a révélé un bug invisible en `jsdom` — la carte en cours de drag était détectée par `document.elementFromPoint` et bloquait son propre drop. Corrigé via `pointer-events: none` sur `.task-card--dragging`. Aucun test unitaire ne l'aurait attrapé.
- **Standardisation des dates appliquée** : `Intl`/ISO (AD-6) utilisé nativement dans le saut de cycle. → application de l'action item n°2 d'Epic 2.
- **Robustesse gagnée par les revues adversariales** : sur 3.2, la revue a détecté un vrai *intent gap* (tâche `priority: null` figée comme un `must`), résolu avec Sylvain (traitée comme `could`), plus un filet de sécurité `setInterval` et un test du chemin d'échec de transaction (AD-7).

## Ce qui a coincé

- **Concurrence "par défaut" à moitié tenue** : l'action item n°3 d'Epic 2 exigeait `isBusy` **+** un index unique en base dès la conception. `isSubmitting` est présent sur les drops, mais l'**index unique Dexie a de nouveau été déféré** (3.1). Report récurrent depuis Epic 2.
- **Trou de vérification manuelle sur 3.2** : la Task 4 (constater le déplacement d'une vraie tâche en navigateur réel) est restée décochée. Les 7 tests couvrent les AC, mais le comportement n'a jamais été observé visuellement.
- **Bruit de revue dû à l'arbre de travail partagé** : des sessions BMAD concurrentes ont modifié DESIGN/EXPERIENCE/ARCHITECTURE-SPINE/epics/.memlog en parallèle ; la revue de 3.3 les a signalés comme faux positifs. Sans worktree isolé par story, le diff depuis `baseline_commit` capture le travail des autres sessions.
- **Backlog `deferred-work.md` qui grossit** : certains items ne sont pas anodins — la confusion case "terminer la tâche" / cases de checklist (repérée au test manuel d'Epic 1) reste ouverte et constitue une vraie chausse-trappe UX (tâches cochées par erreur, disparition irréversible).
- **Logique métier codée en dur dans l'UI** : le seuil de 48h (3.3) et les valeurs de priorité (`should`/`could`, dupliquées entre `cycleJump.js` et `TaskEnrichment.jsx`) vivent dans la couche présentation.

## Suivi de la rétro précédente (Epic 2)

| Action item Epic 2 | Statut | Preuve |
|--------------------|--------|--------|
| 1. Test-First / TDD sur la logique métier complexe | ✅ Complété | `cycleJump.test.js` écrit avant l'impl (3.2) |
| 2. Standardisation des dates (ISO natif) | ✅ Complété | AD-6 appliqué dans `cycleJump.js` |
| 3. Concurrence par défaut (`isBusy` + index unique en base) | ⏳ Partiel | `isSubmitting` présent ; index unique Dexie encore déféré → formalisé en Story 4.7 / NFR8 |

## Découverte significative pour l'Epic 4

- **Bug latent d'intégrité (déféré de 3.2)** : `performDuplicate` (`PlanningView.jsx`) supprime un `PlannedDay` sans réaffecter les tâches qui le référencent → ces tâches deviennent invisibles à la fois du Dépôt et du saut de cycle. La Vue Semaine (4.1) et l'export/sauvegarde JSON (4.3) buteront directement dessus. **À traiter avant 4.1.**
- **La dette est désormais planifiée, pas perdue** : la correction de cap (`sprint-change-proposal-2026-07-05.md`) a transformé les reports en stories — 4.5 (récupération base corrompue / NFR7), 4.7 (fiabilisation écritures + index unique + robustesse tactile / NFR8-9). Pas de mise à jour d'epic supplémentaire requise avant de démarrer l'Epic 4.

## Action items (Epic 4)

1. **Résorber les orphelins `plannedDayId`** avant de démarrer 4.1 : réaffecter/nettoyer les tâches lors de la suppression d'un `PlannedDay`. *(owner : dev/Winston — critique, avant 4.1)*
2. **Établir et appliquer un mini-runbook de vérification navigateur réel** (HTTPS local + contexte sécurisé pour `crypto.randomUUID`) — combler le trou de vérif de 3.2. *(owner : Sylvain/Murat)*
3. **Extraire les constantes métier** (priorités `should`/`could`, seuil 48h) hors des composants UI vers un module partagé. *(owner : dev)*
4. **Un worktree isolé par story** pour éliminer le bruit de revue inter-session. *(owner : Sylvain — process)*
5. **Traiter les items UX-critiques du backlog différé** (au minimum la confusion checkbox/checklist d'Epic 1) avant d'empiler de nouvelles features. *(owner : Sylvain — arbitrage produit)*

## Évaluation de l'état de préparation

- **Code :** Epic 3 mergé dans `main`.
- **Tests :** 94/94 verts, lint et build propres.
- **Vérification manuelle :** saut de cycle (3.2) non observé en navigateur réel — recommandé avant de considérer l'epic définitivement clos.
- **Déploiement :** pas encore en ligne — le CI/CD GitHub Pages est la story 4.6 (backlog).
- **Verdict :** complet du point de vue des stories ; 1 item critique (orphelins `plannedDayId`) et 1 vérif manuelle recommandée avant/pendant l'Epic 4.

## Enseignements clés

1. La vérification en navigateur réel attrape des classes de bugs que `jsdom` masque structurellement (événements tactiles avancés) — la garder obligatoire pour toute interaction tactile.
2. Un report technique répété (l'index unique) finit par devenir un risque nommé (NFR8) — mieux vaut le planifier explicitement que le redéférer.
3. L'arbre de travail partagé entre sessions BMAD génère un coût réel (bruit de revue, risque d'écrasement) — l'isolation par worktree est un investissement de process rentable.
